# Dependências da API. Versões fixadas para que a imagem seja reprodutível, e
# revisadas contra o pip-audit — veja a seção de segurança do README.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic-settings==2.15.0
SQLAlchemy==2.0.53
psycopg[binary]==3.3.5
alembic==1.20.0
minio==7.2.20
# Parser de multipart/form-data, exigido pelo FastAPI para receber uploads.
python-multipart==0.0.32
# Agendador do job de expiração, executado dentro do processo da API.
APScheduler==3.11.3
